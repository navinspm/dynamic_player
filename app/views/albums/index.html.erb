<h1>Listing albums</h1>
<div class="float">
      <% @albums.each do |album| %>
         <td><%= link_to image_tag(album.cover_url(:thumb)), album_path(album) %></td>
    <% end %>
   
 
<%= link_to 'New Album', new_album_path %><br>
<%= link_to 'Video player' , videos_path %><br>
<%= link_to 'Radio' , audios_path %> </div>